export interface Theme {
  colors: {
    bg: {
      gray: string
      white: string
      purple: string
      darkBlue: string
    },
    text: {
      white: string
      grayPrimary: string
      graySecondary: string
    }
  },
  space: {
    gap1: string
    gap2: string
    gap3: string
  }
}